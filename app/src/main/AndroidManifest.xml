<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gigih.wasteoil">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/logoapk"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/logoapk"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".update_data"></activity>
        <activity
            android:name=".MyListData"
            android:theme="@style/splash" />
        <activity
            android:name=".DetailInActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".JualMinyakActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".DetailActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".MasukActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".DaftarActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".SplashscreenActivity"
            android:theme="@style/splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>